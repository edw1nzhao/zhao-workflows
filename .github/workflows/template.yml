name: Template

on:
  workflow_call:
    inputs:
      ENV:
        required: true
        type: string

jobs:
  echo:
    runs-on: ubuntu-latest
    environment: ${{ inputs.ENV }}
    steps:
    - run: |
        if [[ "${{ secrets.NAME }}" == "dev" ]]
        then
          echo deone
        fi

        if [[ "${{ secrets.NAME }}" == "workflow" ]]
        then
          echo stone
        fi

        echo ${{ secrets.NAME }}
