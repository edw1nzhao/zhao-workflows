name: Template

on:
  workflow_call:
    inputs:
      ENV:
        required: true
        type: string

jobs:
  echo:
    runs-on: ubuntu-latest
    environment: ${{ inputs.ENV }}
    steps:
    - run: |
        if [[ "${{ inputs.ENV }}" == "dev" ]]
        then
          echo hi
        fi

        if [[ "${{ inputs.ENV }}" == "workflow" ]]
        then
          echo bye
        fi

        echo ${{ secrets.NAME }}
